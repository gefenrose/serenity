{
  "name": "serenity",
  "version": "1.0.0",
  "private": true,
  "description": "Calm Hebrew journaling companion - PWA + Capacitor native app",
  "scripts": {
    "build": "node build.js",
    "sync": "node build.js && npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
